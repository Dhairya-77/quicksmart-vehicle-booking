<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    tools:context=".common_activities.ForgotPasswordActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginHorizontal="@dimen/_15sdp"
        android:paddingVertical="@dimen/_34sdp">

        <!-- Hero Image -->
        <ImageView
            android:src="@drawable/car_sharing_img"
            android:layout_width="match_parent"
            android:scaleType="fitCenter"
            android:layout_height="@dimen/_200sdp"
            android:contentDescription="@string/app_name" />

        <!-- Title -->
        <TextView
            style="@style/title_style"
            android:text="Forgot Password?" />

        <!-- Subtitle -->
        <TextView
            style="@style/subtitle_style"
            android:text="Enter your registered email and we'll send you a reset link." />

        <!-- Email Input Card -->
        <androidx.cardview.widget.CardView
            style="@style/textbox_card_style"
            android:layout_marginTop="@dimen/_15sdp">

            <EditText
                android:id="@+id/email"
                style="@style/textbox_style"
                android:hint="Enter Email"
                android:inputType="textEmailAddress" />

        </androidx.cardview.widget.CardView>

        <!-- Send Link Button + Progress Bar (toggle visibility in code) -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/_15sdp">

            <Button
                android:id="@+id/btn_send_link"
                style="@style/btn_style"
                android:layout_marginTop="0dp"
                android:text="Send Reset Link" />

            <ProgressBar
                android:id="@+id/progress_bar"
                style="@style/progress_bar_style"
                android:layout_gravity="center"
                android:visibility="gone" />

        </FrameLayout>

        <!-- Back to Login -->
        <TextView
            android:id="@+id/back_to_login"
            style="@style/text_style"
            android:text="Back to Login" />

    </LinearLayout>

</ScrollView>
